{
  "extends": "@tsconfig/node18/tsconfig.json",
  "include": [
    "vite.config.*",
    "vitest.config.*",
    "cypress.config.*",
    "nightwatch.conf.*",
    "playwright.config.*"
  ],
  "compilerOptions": {
    "composite": true, // if removed it solves the include problem
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "types": ["node"]
  }
}
